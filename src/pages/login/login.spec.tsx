import LoginPageDumb from '@pages/login/login.dumb';
import { render } from '@testing-library/react';

describe('Login page', () => {
  it('should be defined.', () => {
    expect(LoginPageDumb).toBeDefined();
  });

  // TODO: Add more tests
});
